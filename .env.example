# Optional — richer news headlines for the "Why is this moving" panel.
# Get a free key at https://finnhub.io/register (60 calls/min free tier).
# Without a key the app falls back to Yahoo Finance news (no key required).
FINNHUB_API_KEY=
